#include <stdio.h>
#include <stdlib.h>

int main(){
	
	int num, fat;
	printf("Digite o numero que deseja ser fatorado: \n");
	scanf(%d, &num);

	for(fat = 1; n > 1; n - 1){
		fat = fat * n;
	}

	printf("\nfatorial do numero %d é = %d ",num,fat);

	return 0;
	system("pause");
}